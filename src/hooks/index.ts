/**
 *
 * /src/hooks/
 *
 * This folder contains all the custom hooks used in the application. You may delete this folder if you don't need custom hooks.
 *
 * useMounted.ts - A custom hook to check if the component is mounted or not.
 * useScrollProgress.ts - A custom hook to track the horizontal (x) and vertical (y) scroll progress of the page.
 *
 */

export * from "./useMounted";
export * from "./useScrollProgress";
